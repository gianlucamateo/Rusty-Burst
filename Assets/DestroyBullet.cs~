using UnityEngine;
using System.Collections;

public class DestroyBullet : MonoBehaviour {

	float destructTime = 2.0f;
	
	// Use this for initialization
	void Start () {

	}
	
	// Update is called once per frame
	void Update () {
		Destroy(gameObject, destructTime);
	}
}
